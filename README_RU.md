# Автопостинг Бот 🤖

Универсальный Python-бот для автоматической публикации и удаления постов с полной поддержкой работы в часовом поясе Новосибирска (NSK).

## 🎯 Основные возможности

✅ **Публикация постов** - создавайте посты в любой момент  
✅ **Автоудаление** - посты удаляются автоматически через указанное время  
✅ **Планирование** - публикуйте посты в конкретное время  
✅ **Преобразование времени** - автоматическое преобразование любого часового пояса в NSK  
✅ **Работа в NSK** - все операции выполняются и планируются в часовом поясе Новосибирска  
✅ **Фоновый планировщик** - использует APScheduler для надежного выполнения задач  
✅ **Хранение данных** - посты сохраняются в JSON файл для восстановления  

## 📦 Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или вручную:

```bash
pip install pytz APScheduler
```

### 2. Структура файлов

```
.
├── bot.py              # Основной класс бота
├── examples.py         # Примеры использования
├── requirements.txt    # Зависимости
├── posts.json         # Хранилище постов (генерируется автоматически)
└── README.md          # Этот файл
```

## 🚀 Быстрый старт

### Базовое использование

```python
from bot import AutoPostBot

# Инициализация бота
bot = AutoPostBot()

# Опубликовать пост сейчас
post_id = bot.publish_post(
    content="Привет, это мой первый пост!",
    delete_after_hours=2  # Удалится через 2 часа
)

print(f"Пост #{post_id} опубликован")
```

### Преобразование времени

```python
from datetime import datetime
import pytz

# Преобразование из московского времени
moscow_time = datetime(2026, 2, 23, 15, 30)
nsk_time = bot.convert_to_nsk_time(moscow_time, from_tz="Europe/Moscow")

print(f"Москва: 15:30 -> NSK: {nsk_time.strftime('%H:%M')}")
```

### Планирование постов

```python
from datetime import datetime, timedelta
import pytz

# Запланировать пост на час вперед
future_time = datetime.now(pytz.UTC) + timedelta(hours=1)

post_id = bot.publish_post_at_time(
    content="Этот пост опубликуется через час!",
    publish_time=future_time,
    from_tz="UTC",
    delete_after_hours=3  # Удалится через 3 часа после публикации
)
```

## 📚 API Документация

### Основной класс: `AutoPostBot`

#### Инициализация

```python
bot = AutoPostBot(storage_file="posts.json")
```

**Параметры:**
- `storage_file` (str): Путь к файлу для хранения постов. По умолчанию `"posts.json"`

---

### Методы

#### `publish_post(content, delete_after_hours=None)`

Опубликовать пост сейчас.

```python
post_id = bot.publish_post(
    content="Содержание поста",
    delete_after_hours=2  # Опционально
)
```

**Параметры:**
- `content` (str): Содержание поста
- `delete_after_hours` (int|float): Удалить через N часов (опционально)

**Возвращает:** ID поста (строка) или None

---

#### `publish_post_at_time(content, publish_time, from_tz=None, delete_after_hours=None)`

Опубликовать пост в указанное время.

```python
from datetime import datetime, timedelta

future_time = datetime.now() + timedelta(hours=1)
post_id = bot.publish_post_at_time(
    content="Содержание",
    publish_time=future_time,
    from_tz="UTC",  # или "Europe/Moscow", "America/New_York" и т.д.
    delete_after_hours=3
)
```

**Параметры:**
- `content` (str): Содержание поста
- `publish_time` (datetime): Когда опубликовать
- `from_tz` (str): Часовой пояс времени (UTC по умолчанию)
- `delete_after_hours` (int|float): Удалить через N часов после публикации

**Возвращает:** ID поста или None

---

#### `delete_post(post_id)`

Удалить пост.

```python
success = bot.delete_post(post_id="1")
```

**Параметры:**
- `post_id` (str): ID поста для удаления

**Возвращает:** True если успешно, False если не найден

---

#### `convert_to_nsk_time(dt, from_tz=None)`

Преобразовать время в часовой пояс Новосибирска.

```python
from datetime import datetime

moscow_time = datetime(2026, 2, 23, 15, 30)
nsk_time = bot.convert_to_nsk_time(moscow_time, from_tz="Europe/Moscow")
```

**Параметры:**
- `dt` (datetime): Объект datetime
- `from_tz` (str): Часовой пояс (UTC по умолчанию)

**Возвращает:** datetime в часовом поясе NSK

---

#### `get_current_nsk_time()`

Получить текущее время в Новосибирске.

```python
nsk_now = bot.get_current_nsk_time()
print(nsk_now.strftime('%Y-%m-%d %H:%M:%S'))
```

**Возвращает:** datetime в часовом поясе NSK

---

#### `get_post(post_id)`

Получить информацию о посте.

```python
post = bot.get_post("1")
if post:
    print(post['content'])
    print(post['status'])  # 'published' или 'scheduled'
```

**Параметры:**
- `post_id` (str): ID поста

**Возвращает:** Словарь с данными поста или None

---

#### `list_posts(status=None)`

Получить список постов.

```python
# Все посты
all_posts = bot.list_posts()

# Только опубликованные
published = bot.list_posts(status='published')

# Только запланированные
scheduled = bot.list_posts(status='scheduled')

for post in all_posts:
    print(f"#{post['id']}: {post['content']}")
```

**Параметры:**
- `status` (str): Фильтр: `'published'`, `'scheduled'` или None для всех

**Возвращает:** Список словарей с данными постов

---

#### `get_jobs_info()`

Получить информацию о запланированных заданиях.

```python
jobs = bot.get_jobs_info()
for job in jobs:
    print(f"{job['id']}: {job['next_run_time']}")
```

**Возвращает:** Список словарей с информацией о заданиях

---

#### `shutdown()`

Остановить бота (завершить планировщик).

```python
bot.shutdown()
```

---

## 🌍 Поддерживаемые часовые пояса

Вот некоторые часовые пояса, которые можно использовать:

```python
# Россия
"Europe/Moscow"          # Москва (UTC+3)
"Asia/Novosibirsk"       # Новосибирск (UTC+7) - основной
"Asia/Yekaterinburg"     # Екатеринбург (UTC+5)
"Asia/Vladivostok"       # Владивосток (UTC+10)

# Европа
"Europe/London"          # Лондон (UTC+0/+1)
"Europe/Paris"           # Париж (UTC+1/+2)
"Europe/Berlin"          # Берлин (UTC+1/+2)

# Азия
"Asia/Tokyo"             # Токио (UTC+9)
"Asia/Shanghai"          # Шанхай (UTC+8)
"Asia/Kolkata"           # Индия (UTC+5:30)

# Америка
"America/New_York"       # Нью-Йорк (UTC-5/-4)
"America/Los_Angeles"    # Лос-Анджелес (UTC-8/-7)
"America/Toronto"        # Торонто (UTC-5/-4)

# Австралия
"Australia/Sydney"       # Сидней (UTC+10/+11)

# UTC
"UTC"                    # Всемирное координированное время
```

Полный список: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

---

## 💾 Формат хранилища (posts.json)

Посты сохраняются в JSON формате:

```json
{
  "1": {
    "id": "1",
    "content": "Содержание поста",
    "published_at": "2026-02-23T15:30:00+07:00",
    "status": "published"
  },
  "2": {
    "id": "2",
    "content": "Запланированный пост",
    "scheduled_for": "2026-02-23T16:00:00+07:00",
    "status": "scheduled",
    "delete_after_hours": 3
  }
}
```

---

## 📝 Примеры использования

### Пример 1: Пост с автоудалением

```python
# Пост удалится через 30 минут
bot.publish_post(
    content="Временный пост",
    delete_after_hours=0.5
)
```

### Пример 2: Публикация завтра в конкретное время

```python
from datetime import datetime, timedelta
import pytz

# Завтра в 10:00 NSK
tomorrow_10am_nsk = (
    datetime.now(pytz.UTC) + timedelta(days=1)
).replace(hour=7, minute=0, second=0)

bot.publish_post_at_time(
    content="Доброе утро!",
    publish_time=tomorrow_10am_nsk,
    from_tz="UTC"
)
```

### Пример 3: Преобразование времени из встречи

```python
# Встреча назначена на 14:00 по московскому времени
meeting_time = datetime(2026, 2, 25, 14, 0)
nsk_time = bot.convert_to_nsk_time(meeting_time, "Europe/Moscow")

# Опубликовать напоминание в NSK
bot.publish_post_at_time(
    content=f"Встреча начинается через час (в {nsk_time.strftime('%H:%M')} NSK)!",
    publish_time=nsk_time - timedelta(hours=1),
    from_tz="UTC"
)
```

### Пример 4: Полный цикл работы

```python
from datetime import datetime, timedelta

# 1. Получить текущее время
now = bot.get_current_nsk_time()
print(f"Текущее время: {now.strftime('%Y-%m-%d %H:%M:%S')}")

# 2. Опубликовать пост
post_id = bot.publish_post(
    content="Новый пост!",
    delete_after_hours=1
)

# 3. Проверить статус
post = bot.get_post(post_id)
print(f"Статус: {post['status']}")

# 4. Показать все посты
for post in bot.list_posts():
    print(f"#{post['id']}: {post['content']}")

# 5. Запланировать удаление
bot.delete_post(post_id)
```

---

## 🔧 Запуск примеров

```bash
python examples.py
```

Примеры включают:
- Базовое использование
- Преобразование времени
- Планирование постов
- Управление постами
- Сравнение часовых поясов
- Симуляцию работы

---

## 🛠️ Продвинутые возможности

### Использование в других приложениях

```python
from bot import AutoPostBot
from flask import Flask

app = Flask(__name__)
bot = AutoPostBot()

@app.route('/publish', methods=['POST'])
def publish():
    content = request.json['content']
    delete_after = request.json.get('delete_after_hours')
    post_id = bot.publish_post(content, delete_after)
    return {'post_id': post_id}

@app.route('/posts')
def get_posts():
    return bot.list_posts()
```

### Интеграция с базой данных

```python
# Легко можно модифицировать для использования с БД
# вместо JSON файла
class DatabaseAutoPostBot(AutoPostBot):
    def _load_posts(self):
        # Загрузить из БД
        pass
    
    def _save_posts(self):
        # Сохранить в БД
        pass
```

---

## 🐛 Логирование

Бот использует встроенный модуль logging. Все операции логируются с уровнями:
- **INFO** - основные операции (публикация, удаление)
- **WARNING** - предупреждения (пост не найден)
- **ERROR** - ошибки

Логи выводятся в консоль с меткой времени.

---

## ⚙️ Конфигурация

### Изменение часового пояса

Если нужно работать с другим часовым поясом, измените строку в файле bot.py:

```python
self.nsk_tz = pytz.timezone('Asia/Novosibirsk')
```

На нужный пояс:

```python
self.nsk_tz = pytz.timezone('Europe/Moscow')  # Москва
# или
self.nsk_tz = pytz.timezone('Asia/Tokyo')     # Токио
```

### Изменение файла хранилища

```python
bot = AutoPostBot(storage_file="my_posts.json")
```

---

## 📊 Безопасность

⚠️ **Важно:** Этот бот использует JSON файл для хранения. Для производства рекомендуется:

- Использовать БД (PostgreSQL, MongoDB)
- Добавить аутентификацию
- Валилировать входные данные
- Использовать HTTPS для API
- Добавить шифрование чувствительных данных

---

## 🤝 Расширение функциональности

### Добавление Telegram интеграции

```python
from telegram import Bot

class TelegramAutoPostBot(AutoPostBot):
    def __init__(self, telegram_token, chat_id):
        super().__init__()
        self.bot = Bot(token=telegram_token)
        self.chat_id = chat_id
    
    def publish_post(self, content, **kwargs):
        post_id = super().publish_post(content, **kwargs)
        # Отправить в Telegram
        self.bot.send_message(
            chat_id=self.chat_id,
            text=f"Пост #{post_id}: {content}"
        )
        return post_id
```

### Добавление уведомлений по email

```python
import smtplib

class EmailNotificationBot(AutoPostBot):
    def publish_post(self, content, **kwargs):
        post_id = super().publish_post(content, **kwargs)
        # Отправить email
        self._send_email(f"Пост #{post_id} опубликован")
        return post_id
    
    def _send_email(self, message):
        # Реализация отправки email
        pass
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что установлены зависимости: `pip install -r requirements.txt`
3. Убедитесь, что есть доступ к файлу `posts.json`
4. Проверьте правильность часовых поясов

---

## 📄 Лицензия

Этот проект свободно распространяется. Используйте на свое усмотрение.

---

## 🎉 Версия

**v1.0.0** - Первая версия с основной функциональностью

---

**Создано:** 2026-02-23  
**Часовой пояс:** Asia/Novosibirsk (UTC+7)
